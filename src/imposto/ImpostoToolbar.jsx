import React from "react";
import { SaveButton, Toolbar } from "react-admin";

const ImpostoToolbar = props => (
  <Toolbar {...props}>
    <SaveButton />
  </Toolbar>
);

export default ImpostoToolbar;
